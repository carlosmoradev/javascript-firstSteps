// prueba para calcular el area de un rectangulo usando arrow functions

let base = 10

let altura = 30

let calculaRectangulo = (base, altura) => base * altura

console.log(`El área del rectangulo es ${calculaRectangulo(base, altura)}`)